import { FilterTechPipe } from './filter-tech.pipe';

describe('FilterTechPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTechPipe();
    expect(pipe).toBeTruthy();
  });
});
